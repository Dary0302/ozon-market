using Microsoft.EntityFrameworkCore;

namespace OzonMarket.Domain;

public class Bucket : BaseEntity
{
    /*[BackingField(nameof(photoList))]
    public IReadOnlyCollection<HuntPhoto> PhotoList => photoList.AsReadOnly();
    
    private readonly List<HuntPhoto> photoList = [];*/
}